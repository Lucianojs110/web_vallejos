---
import { getFeaturedProductsPage1, getFeaturedProductsPage2 } from "../../services";
import ProductCard from "../Catalog/ProductCard.astro";


// Llamar al endpoint correcto para obtener los productos destacados
const data = await getFeaturedProductsPage2();
const products = data?.products?.slice(0, 8) || []; // Limitar a 8 productos si existen
---


<div class="!text-gray-500 flex justify-center items-center p-6 font-bold text-xl">
    MÁS PRODUCTOS
</div>

<div class="w-full bg-white p-6">
    <div class="grid grid-cols-2 gap-2 sm:gap-4 sm:grid-cols-3 lg:grid-cols-4 w-full md:w-4/5 xl:w-3/4 mx-auto">
        {products.map((product) => (
            <ProductCard data={product} />
        ))}
    </div>
</div>